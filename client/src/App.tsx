import React from "react";
import NumberToWordsForm from "./components/NumberToWordsForm";

function App(): React.ReactElement {
  return (
    <div className="flex items-center justify-center h-screen">
      <NumberToWordsForm />
    </div>
  );
}

export default App;
